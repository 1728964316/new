#include<stdio.h>
int main()
{
	int a;
	int b = 0;
	int c = 0;
	scanf("%d", &a);
	while (a != -1)
	{
		if (a%2==0)
		{
			b++;//偶数
		}
		else
		{
			c++;//奇数
		}
		scanf("%d", &a);
	}
	printf("%d %d", c, b);
	return 0;
	
}
